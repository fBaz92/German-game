# Database Configuration
# Se non specifichi DATABASE_URL, verrà usato SQLite locale (game_history.db)

# Per PostgreSQL, specifica l'URL completo:
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Esempio locale PostgreSQL:
# DATABASE_URL=postgresql://postgres:mypassword@localhost:5432/german_game

# Esempio Heroku/Railway/Render (viene fornito automaticamente):
# DATABASE_URL=postgresql://user:pass@ec2-xxx.compute-1.amazonaws.com:5432/dbname

